import { Space } from './space/Space';
export interface DebugInfo {
    participantId: string;
    sessionId: string;
    spaceId: string;
    connectionId: string;
    localParticipantRole: string;
}
export declare class Debugger {
    space: Space;
    logs: string[];
    constructor(space: Space);
    debug(): DebugInfo;
}

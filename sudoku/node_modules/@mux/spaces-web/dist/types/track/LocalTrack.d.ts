import { Track } from './Track';
export declare class LocalTrack extends Track {
    /**
     * DeviceID of the device that is currently being used for this track
     *
     * **Note** deviceId is not supported for screen shares in Firefox and will be undefined
     */
    deviceId?: string;
    track: MediaStreamTrack;
    mute(): void;
    unMute(): void;
    /**
     * Stop capturing media from the track.
     */
    stop(): void;
}

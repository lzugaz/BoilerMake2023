export declare enum TrackEvent {
    /**
     * When the track has been muted
     *
     * args: ()
     */
    Muted = "Muted",
    /**
     * When the track has been unmuted
     *
     * args: ()
     */
    Unmuted = "Unmuted"
}
